import * as React from "react";

interface Props {}

const Todo: React.FC<Props> = (props: Props) => {
  return (
    <div className="todo">
      <p>a todo</p>
    </div>
  );
};
export default Todo;
